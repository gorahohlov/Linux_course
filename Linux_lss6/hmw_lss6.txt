1.
echo 'line1
line2

line3
line4


line5

line6
' > line_f.txt
cat line_f.txt
cat line_f.txt | tr -s '\n\n' > line_f2.txt
cat line_f2.txt
# all correct
# --
cat line_f.txt | tr -s '\n' | tr a-z A-Z > line_f2.txt
cat line_f2.txt

mkdir ~/bin
export PATH=~/bin:"PATH"
cd bin
vim tsk_1.sh
#!/bin/bash

cat ~/line_f.txt | tr -s '\n' | tr a-z A-Z > ~/line_f2.txt
cat ~/line_f2.txt
#:wq
chmod 775 tst_1.sh
tsk_1.sh

2.

остальные задания не успел. Прошу времени доделать позже.
